# profiles — 他者理解タイプ正本（OtherTypeEntry）

`OtherTypeEntry`（`../schema.ts`）に準拠した翼／タイプ辞書の置き場。

## 状態

| ファイル | 状態 |
|----------|------|
| `type1w2.ts` (`Type_1w2`) | **② 投入済み**（救済・正義／1w9対比補強） |
| `type1w9.ts` (`Type_1w9`) | **② 再昇格済み**（日常〜健全主軸／応力は下書き対照のみ） |
| `type2w1.ts` (`Type_2w1`) | **② 再昇格済み**（日常〜健全主軸／応力は下書き対照のみ） |
| `type2w3.ts` (`Type_2w3`) | **② 再昇格済み**（日常〜健全主軸／応力は下書き対照のみ） |
| `type3w2.ts` (`Type_3w2`) | **② 再昇格済み**（日常〜健全主軸／影は下書き対照のみ） |
| `type3w4.ts` (`Type_3w4`) | **② 再昇格済み**（健全〜平均主軸／影は下書き対照のみ） |
| `type4w3.ts` (`Type_4w3`) | **② 投入済み**（標準4寄り代表） |
| `type4w5.ts` (`Type_4w5`) | **② 再昇格済み**（日常〜健全主軸／極限は下書き対照のみ） |
| `type5w4.ts` (`Type_5w4`) | **② 再昇格済み**（日常〜健全主軸／強度は下書き対照のみ） |
| `type5w6.ts` (`Type_5w6`) | **② 投入済み**（信玄型＋折木型の統合） |
| `type6w5.ts` (`Type_6w5`) | **② 投入済み**（ホームズ型／5w6競合注記あり） |
| `type6w7.ts` (`Type_6w7`) | **② 再昇格済み**（日常〜健全主軸／極度は下書き対照のみ） |
| `type7w6.ts` (`Type_7w6`) | **② 再昇格済み**（日常〜健全主軸／強度は下書き対照のみ） |
| `type7w8.ts` (`Type_7w8`) | **② 再昇格済み**（日常〜健全主軸／強度は下書き対照のみ） |
| `type8w7.ts` (`Type_8w7`) | **② 投入済み**（潔型／強さのための柔軟性） |
| `type8w9.ts` (`Type_8w9`) | **② 再昇格済み**（日常〜健全主軸／応力は下書き対照のみ） |
| `type9w1.ts` (`Type_9w1`) | **② 投入済み**（夏目型／静かな正しさ） |
| `type9w8.ts` (`Type_9w8`) | **② 投入済み**（達也型／9w1競合注記あり） |
| その他 | 未投入（下書き充実後に②） |

既存 `../wings/*.ts` は当面 self 再エクスポートのまま（移行中）。

## 偏りについて

現状の投入は、観察者が「光」より構造的な摩擦・弱い側面を多く見ている標本が多い。  
平均像ではないが、**同じ構造を体験している他者理解**には有用、という方針で昇格している。  
ファイル先頭コメントに偏り注記がある場合はそれを優先する。

## ルール

- `_drafts` の固有名詞・物語は入れない
- 昇格は翼ごとに別指示で行う
- C欄（相互理解シード）は本ディレクトリに入れない → relationship ④⑤
- 下書きは `_TEMPLATE.md` の **共通識別メモ**（姉妹翼／競合／健全時／本能）を推奨
- 姉妹対比の例: `_drafts/wings/_PAIR_2w1_2w3.md`

## 底上げメモ（2026-08）

感情センター初期組と1w2を後期水準に近づけるため、対比・健全帯・テンプレ共通欄を追加済み。