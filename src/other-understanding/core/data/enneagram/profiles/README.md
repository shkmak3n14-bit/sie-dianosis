# profiles — 他者理解タイプ正本（OtherTypeEntry）

`OtherTypeEntry`（`../schema.ts`）に準拠した翼／タイプ辞書の置き場。

## 状態

| ファイル | 状態 |
|----------|------|
| `type1w2.ts` (`Type_1w2`) | **② 投入済み** |
| `type2w1.ts` (`Type_2w1`) | **② 投入済み** |
| `type2w3.ts` (`Type_2w3`) | **② 投入済み** |
| `type3w2.ts` (`Type_3w2`) | **② 投入済み** |
| `type3w4.ts` (`Type_3w4`) | **② 投入済み**（弱い側面寄り標本） |
| `type4w3.ts` (`Type_4w3`) | **② 投入済み** |
| `type4w5.ts` (`Type_4w5`) | **② 投入済み**（極限ダーク作品標本） |
| その他 | 未投入（下書き充実後に②） |

既存 `../wings/*.ts` は当面 self 再エクスポートのまま（移行中）。

## 偏りについて

現状の投入は、観察者が「光」より構造的な摩擦・弱い側面を多く見ている標本が多い。  
平均像ではないが、**同じ構造を体験している他者理解**には有用、という方針で昇格している。  
ファイル先頭コメントに偏り注記がある場合はそれを優先する。

## ルール

- `_drafts` の固有名詞・物語は入れない
- 昇格は翼ごとに別指示で行う
- C欄（相互理解シード）は本ディレクトリに入れない → relationship ④⑤
